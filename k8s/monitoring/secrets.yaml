# Monitoring secrets - Grafana admin password
# Note: In production, use proper secret management (e.g., Sealed Secrets, Vault)
apiVersion: v1
kind: Secret
metadata:
  name: monitoring-secrets
  namespace: research-dashboard
  labels:
    app: monitoring
type: Opaque
stringData:
  grafana-admin-password: "admin123"  # Change this in production!
